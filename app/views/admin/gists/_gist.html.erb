<tr>
  <td><%= "#{gist.author.email}" %></td>
  <td><%= link_to truncate(gist.question.body, :length => 28 ),admin_question_path(gist.question)  %></td>
  <td><%= link_to gist.url.split('/').last, gist.url %></td>
</tr>
