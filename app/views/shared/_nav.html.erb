<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container">
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
    
    <% if current_user && current_user.admin? %>
      <%= link_to t('.admin_panel'), admin_tests_path, class: 'navbar-brand' %>
    <% end %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <span class="navbar-text">
        <% if user_signed_in? %>
          <%= t('.welcome') %>, <b><%= current_user.email %></b> Guru |
          <%= button_to t('.logout'), destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to t('.log_in'), new_session_path(resource_name) %> |
          <%= link_to t('.sign_up'), new_registration_path(resource_name) %>
        <% end %>
      </span>
    </div>
  </div>
</nav>
