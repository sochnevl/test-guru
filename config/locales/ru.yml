ru:
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              blank: "не может быть пустым"
              invalid: "некорректный формат"
            password:
              blank: "не может быть пустым"
            password_confirmation:
              confirmation: "не совпадает с паролем"
        test:
          attributes:
            category:
              required: "не может быть пустым"
            author:
              required: "не может быть пустым"
            title:
              blank: "не может быть пустым"
        question:
          attributes:
            body:
              blank: "не может быть пустым"
      messages:
        record_invalid: "Ошибка сохранения записи"
  hello: "Привет, мир"
